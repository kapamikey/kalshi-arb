# Dashboard

Read-only Vite + React viewer. Full notes are in the repo README Dashboard section.

```bash
cd web
cp .env.example .env
bun install
bun dev
```

http://localhost:5173

`VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` (anon/publishable only).
